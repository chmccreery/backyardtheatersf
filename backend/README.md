All instructions should be executed from inside this directory.

To add a dependency:

```
poetry add <dependency>
```

To install the defined dependencies:

```
poetry install
```

To run the backend:

```
poetry run python src/main.py
```
